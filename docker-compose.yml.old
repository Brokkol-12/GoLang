services:
  postgres:
    container_name: postrgers_go
    image: postgres:16.4
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: sadmin
      PGDATA: /data/postgress
    volumes:
      - ./postgres-data:/data/postgres
    ports:
      - "5432:5432" 